# Constructor for `getGBIF` Objects

Wrap a data frame in the `getGBIF` class used by downstream functions in
the package.

## Usage

``` r
getGBIF(df)
```

## Arguments

- df:

  A data frame containing GBIF occurrence records.

## Value

An object of class `getGBIF`.
