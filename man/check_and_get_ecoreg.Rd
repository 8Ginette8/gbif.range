% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_ecoreg.R
\name{check_and_get_ecoreg}
\alias{check_and_get_ecoreg}
\title{Check for a Local Ecoregion Layer and Download It if Needed}
\usage{
check_and_get_ecoreg(ecoreg_name = "eco_terra", save_dir = NULL)
}
\arguments{
\item{ecoreg_name}{Character. File name of the ecoregion dataset to check. See
\code{ecoreg_list} for valid values.}

\item{save_dir}{Character. Directory where downloaded files should be stored.
Defaults to \code{tempdir()} unless the \code{gbif.range.save_dir} option or
\code{GBIF_RANGE_SAVE_DIR} environment variable is set, in which case that
location is used instead.}
}
\value{
\code{NULL}. The function downloads data only when required.
}
\description{
Check whether a target ecoregion directory exists and contains at least one
shapefile. If not, download the dataset with \code{get_ecoreg()}.
}
\examples{
\donttest{
check_and_get_ecoreg("eco_terra", save_dir = tempdir())
}
}
\seealso{
\code{\link{get_ecoreg}}() to force a download, and
\code{\link{read_ecoreg}}() which calls this function internally before
loading the layer.
}
