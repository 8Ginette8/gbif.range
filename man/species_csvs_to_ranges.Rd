% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gbif_file_workflows.R
\name{species_csvs_to_ranges}
\alias{species_csvs_to_ranges}
\title{Build Range Maps from Per-Species GBIF Files Saved on Disk}
\usage{
species_csvs_to_ranges(
  species_dir,
  ecoreg,
  ecoreg_name = NULL,
  outdir = file.path(tempdir(), "gbif_ranges"),
  occ_outdir = NULL,
  occ_save_as = c("none", "tsv", "rds"),
  range_save_as = c("rds", "gpkg", "tif"),
  deduplicate = TRUE,
  sep_in = "\\t",
  overwrite = FALSE,
  verbose = TRUE,
  ...
)
}
\arguments{
\item{species_dir}{Directory containing files created by
\code{\link{split_gbif_by_species}()}.}

\item{ecoreg}{Ecoregion input accepted by \code{\link{get_range}()}. This
can be a spatial object already loaded in memory, or a built-in ecoregion
name such as \code{"eco_terra"} that will be resolved with
\code{\link{read_ecoreg}()}.}

\item{ecoreg_name}{Name of the categorical field defining the ecoregion
units. For example, use \code{"ECO_NAME"} with \code{"eco_terra"}.}

\item{outdir}{Directory where range files will be saved.}

\item{occ_outdir}{Optional directory where the minimal occurrence tables
passed to \code{\link{get_range}()} will also be saved.}

\item{occ_save_as}{File format for minimal occurrence tables. Choose among
\code{"none"}, \code{"tsv"}, or \code{"rds"}.}

\item{range_save_as}{File format for range outputs. Choose among
\code{"rds"}, \code{"gpkg"}, or \code{"tif"}.}

\item{deduplicate}{Logical. Should identical longitude-latitude pairs be
collapsed before range inference?}

\item{sep_in}{Field separator used by the per-species occurrence files.}

\item{overwrite}{Logical. If \code{TRUE}, existing outputs in
\code{outdir}/\code{occ_outdir} are replaced.}

\item{verbose}{Logical. Should progress messages be printed?}

\item{...}{Additional arguments passed to \code{\link{get_range}()}.}
}
\value{
A data frame summarizing the processed files, with one row per range
  and the columns \code{species_key}, \code{species_name}, \code{n_points},
  \code{occ_file}, and \code{range_file}.
}
\description{
Read one occurrence file per species, prepare the minimal coordinate table
needed by \code{\link{get_range}()}, and save one range output per species.
}
\details{
The function is intended to work seamlessly with
  \code{\link{split_gbif_by_species}()}. It reads each species file
  sequentially, adds the single-species \code{input_search} column expected
  by \code{get_range()}, and keeps only the coordinate columns needed for
  range construction.
}
\examples{
\dontrun{
if (requireNamespace("data.table", quietly = TRUE)) {
  gbif_file <- system.file("extdata", "occ_example_2sps.csv", package = "gbif.range")
  split_dir <- file.path(tempdir(), "gbif_species_help")
  occ_dir <- file.path(tempdir(), "gbif_occ_help")
  range_dir <- file.path(tempdir(), "gbif_range_help")

  unlink(split_dir, recursive = TRUE)
  unlink(occ_dir, recursive = TRUE)
  unlink(range_dir, recursive = TRUE)

  split_gbif_by_species(
    input_file = gbif_file,
    outdir = split_dir,
    chunk_size = 10,
    sep_in = "\t",
    sep_out = "\t",
    overwrite = TRUE,
    verbose = FALSE
  )

  # Crop the packaged terrestrial ecoregions to the extent of the example
  # occurrences so the help example stays lightweight.
  occ_example <- utils::read.delim(gbif_file, sep = "\t", stringsAsFactors = FALSE)
  eco_terra <- read_ecoreg("eco_terra")
  eco_crop <- terra::crop(
    eco_terra,
    terra::ext(
      min(occ_example$decimalLongitude) - 5,
      max(occ_example$decimalLongitude) + 5,
      min(occ_example$decimalLatitude) - 5,
      max(occ_example$decimalLatitude) + 5
    )
  )

  range_summary <- species_csvs_to_ranges(
    species_dir = split_dir,
    ecoreg = eco_crop,
    ecoreg_name = "ECO_NAME",
    outdir = range_dir,
    occ_outdir = occ_dir,
    occ_save_as = "tsv",
    range_save_as = "rds",
    sep_in = "\t",
    overwrite = TRUE,
    degrees_outlier = 30,
    clust_pts_outlier = 2,
    buff_width_point = 1,
    buff_incrmt_pts_line = 0.1,
    buff_width_polygon = 1,
    raster = FALSE,
    format = "SpatVector",
    verbose = FALSE
  )

  range_summary[, c("species_name", "n_points", "range_file")]

  # The same batch call can also resolve the built-in layer internally:
  # species_csvs_to_ranges(species_dir = split_dir, ecoreg = "eco_terra", ...)
}
}
}
\seealso{
\code{\link{split_gbif_by_species}()} and
  \code{\link{read_range_rds}()}.
}
