% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/make_blocks.R
\name{make_blocks}
\alias{make_blocks}
\title{Split Data into Approximately Balanced Folds}
\usage{
make_blocks(
  nfolds = 5,
  df = data.frame(),
  nblocks = nfolds * 2,
  npoints = NA,
  pres = numeric()
)
}
\arguments{
\item{nfolds}{Numeric number of folds to create.}

\item{df}{Optional \code{data.frame} whose columns define the structure used
to build folds. When omitted, random folds are created from
\code{npoints}.}

\item{nblocks}{Numeric number of initial clusters used to build the folds.
Must be at least \code{nfolds}.}

\item{npoints}{Optional number of observations to split when \code{df} is
not supplied.}

\item{pres}{Optional binary vector used to restrict CLARA clustering to a
subset of rows and assign the remainder by nearest neighbours.}
}
\value{
An integer vector of length \code{nrow(df)} or \code{npoints},
giving the fold assignment for each observation.
}
\description{
Create a fold-assignment vector for random or spatially structured
cross-validation.
}
\details{
If \code{df} has one column, folds are based on quantile bins. If
\code{df} has two or more columns, folds are based on CLARA clustering.
Remaining clusters are assigned to folds with an optimization step that
tries to balance fold sizes as evenly as possible.
}
\examples{
\dontrun{
# Downloading worldwide the observations of Panthera tigris
obs.pt <- get_gbif(
    sp_name = "Panthera tigris",
    basis = c("OBSERVATION","HUMAN_OBSERVATION",
        "MACHINE_OBSERVATION","OCCURRENCE")
)

# Create a vector of folds (n = 5) spatially blocked (n = 10)
block.pt <- make_blocks(
    nfolds = 5,
    df = obs.pt[, c("decimalLatitude","decimalLongitude")],
    nblocks = 10
)

# Plot one colour per fold
countries <- rnaturalearth::ne_countries(
    type = "countries",
    returnclass = "sv"
)
countries.focus <- terra::crop(
    countries,
    terra::ext(60,100,0,40)
)
terra::plot(countries.focus, col = "#bcbddc")
graphics::points(
    obs.pt[, c("decimalLongitude","decimalLatitude")],
    pch = 20,
    col = block.pt,
    cex = 1
)
}
}
\references{
Brun, P., Thuiller, W., Chauvier, Y., Pellissier, L., Wüest, R. O.,
Wang, Z., & Zimmermann, N. E. (2020). Model complexity affects species
distribution projections under climate change. Journal of Biogeography,
47(1), 130-142.
}
